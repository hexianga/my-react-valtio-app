# 分支保护工具

## 📋 目录

- [功能说明](#功能说明)
- [实现原理](#实现原理)
- [使用方法](#使用方法)
- [配置说明](#配置说明)
- [测试验证](#测试验证)
- [常见问题](#常见问题)

## 功能说明

本工具用于**禁止本地向 master 和 release 打头的分支推送代码**，强制开发人员使用 Pull Request 流程，保护重要分支的代码质量。

### 受保护的分支

默认情况下，以下分支受到保护：

- ✅ `master` - 主分支
- ✅ `main` - 主分支（GitHub 默认）
- ✅ `release/*` - 所有以 release/ 开头的分支（如 `release/v1.0`）
- ✅ `release-*` - 所有以 release- 开头的分支（如 `release-1.0`）

### 允许推送的分支

其他分支可以正常推送，例如：

- ✅ `feature/*` - 功能分支
- ✅ `bugfix/*` - 修复分支
- ✅ `hotfix/*` - 热修复分支
- ✅ `develop` - 开发分支
- ✅ 其他自定义分支

## 实现原理

### 技术架构

```
┌─────────────────┐
│  git push       │  用户执行推送命令
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  pre-push hook  │  Git 自动触发 hook
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  branch-protection.js               │
│  ┌───────────────────────────────┐  │
│  │ 1. 读取 stdin 获取推送信息   │  │
│  │ 2. 解析目标分支名称           │  │
│  │ 3. 检查是否匹配保护规则       │  │
│  │ 4. 返回退出码                 │  │
│  └───────────────────────────────┘  │
└────────┬────────────────────────────┘
         │
         ▼
    ┌────┴────┐
    │ 退出码  │
    └────┬────┘
         │
    ┌────┴────────┐
    │             │
    ▼             ▼
  0 (成功)      1 (失败)
    │             │
    ▼             ▼
 允许推送      拒绝推送
```

### 核心原理

#### 1. Git Hooks 机制

Git Hooks 是 Git 在特定事件发生时自动执行的脚本。本工具使用 **pre-push hook**：

- **触发时机**：在 `git push` 执行前，但在数据传输到远程仓库之前
- **位置**：`.git/hooks/pre-push`（通过 husky 管理在 `.husky/pre-push`）
- **作用**：可以检查推送内容并决定是否允许推送

#### 2. 标准输入数据格式

Git 会通过标准输入（stdin）向 pre-push hook 传递推送信息，格式为：

```
<local ref> <local sha1> <remote ref> <remote sha1>
```

示例：

```
refs/heads/feature/new-feature 67890abcdef refs/heads/master 12345abcdef
```

字段说明：

- `local ref`: 本地分支引用（如 `refs/heads/feature/new-feature`）
- `local sha1`: 本地提交的 SHA1 哈希值
- `remote ref`: 远程分支引用（如 `refs/heads/master`）**← 这是我们要检查的目标分支**
- `remote sha1`: 远程提交的 SHA1 哈希值

#### 3. 分支检查逻辑

```javascript
// 1. 解析分支名
const remoteRef = parts[2]; // 获取远程引用
const branchName = remoteRef.replace(/^refs\/heads\//, ''); // 移除前缀

// 2. 精确匹配检查
if (PROTECTED_BRANCHES.exact.includes(branchName)) {
  return true; // master, main
}

// 3. 前缀匹配检查
if (PROTECTED_BRANCHES.prefixes.some(prefix => branchName.startsWith(prefix))) {
  return true; // release/*, release-*
}
```

#### 4. 退出码控制

- **退出码 0**：表示成功，Git 继续执行推送
- **退出码 1**（或任何非零值）：表示失败，Git 中止推送

```javascript
if (isProtectedBranch(remoteRef)) {
  printError(branchName);
  process.exit(1); // 中止推送
}

console.log('✓ 分支检查通过');
process.exit(0); // 允许推送
```

## 使用方法

### 自动安装

项目已配置好 husky，当你运行 `npm install` 时会自动安装 Git hooks：

```bash
cd my-react-valtio-app
npm install
```

### 验证安装

检查 hook 是否正确安装：

```bash
# 查看 pre-push hook
cat .husky/pre-push

# 应该看到：
# #!/usr/bin/env sh
# . "$(dirname -- "$0")/_/husky.sh"
# node scripts/branch-protection.js
```

### 实际使用场景

#### ✅ 正确的工作流程

```bash
# 1. 创建功能分支
git checkout -b feature/user-authentication

# 2. 开发并提交代码
git add .
git commit -m "feat: add user authentication"

# 3. 推送到功能分支（✅ 允许）
git push origin feature/user-authentication

# 4. 在 GitHub/GitLab 创建 Pull Request
# 5. 代码审查通过后，通过 PR 合并到 master
```

#### ❌ 错误的操作（会被拒绝）

```bash
# 切换到 master 分支
git checkout master

# 尝试直接推送到 master（❌ 被拒绝）
git push origin master

# 输出：
# ╔════════════════════════════════════════════════════════════╗
# ║           🚫  分支保护：推送被拒绝                        ║
# ╚════════════════════════════════════════════════════════════╝
#
# 目标分支：master
# ❌ 禁止直接推送到以下分支：
#    • master
#    • main
#    • release/* (所有 release 开头的分支)
```

## 配置说明

### 修改保护规则

编辑 `scripts/branch-protection.js` 文件中的配置：

```javascript
const PROTECTED_BRANCHES = {
  exact: ['master', 'main'], // 精确匹配的分支名
  prefixes: ['release/', 'release-'], // 前缀匹配的分支名
};
```

#### 示例：添加更多保护分支

```javascript
const PROTECTED_BRANCHES = {
  exact: ['master', 'main', 'production', 'staging'],
  prefixes: ['release/', 'release-', 'hotfix/'],
};
```

#### 示例：只保护 master

```javascript
const PROTECTED_BRANCHES = {
  exact: ['master'],
  prefixes: [],
};
```

### 自定义错误信息

修改 `printError` 函数中的提示信息：

```javascript
function printError(branchName) {
  console.error('您的自定义错误信息');
  // ...
}
```

## 测试验证

### 运行自动化测试

项目提供了完整的测试套件：

```bash
cd my-react-valtio-app
node scripts/test-branch-protection.js
```

测试输出示例：

```
╔════════════════════════════════════════════════════════════╗
║          分支保护工具 - 测试套件                          ║
╚════════════════════════════════════════════════════════════╝

测试: ✅ 允许推送到 feature 分支
✓ 分支检查通过
✓ 测试通过

测试: ❌ 禁止推送到 master 分支
🚫  分支保护：推送被拒绝
✓ 测试通过

...

测试总结:
  ✓ 通过: 7
  ✗ 失败: 0
  总计: 7

🎉 所有测试通过！
```

### 手动测试

#### 测试 1：推送到受保护分支（应该失败）

```bash
# 创建测试提交
git checkout master
echo "test" > test.txt
git add test.txt
git commit -m "test"

# 尝试推送（会被拒绝）
git push origin master

# 清理测试
git reset --hard HEAD~1
```

#### 测试 2：推送到普通分支（应该成功）

```bash
# 创建功能分支
git checkout -b feature/test-branch-protection

# 创建测试提交
echo "test" > test.txt
git add test.txt
git commit -m "test: branch protection"

# 推送（应该成功）
git push origin feature/test-branch-protection

# 清理
git checkout main
git branch -D feature/test-branch-protection
```

## 常见问题

### Q1: 如何临时绕过分支保护？

**A:** 有几种方法：

1. **使用 `--no-verify` 标志**（不推荐）：

   ```bash
   git push --no-verify origin master
   ```

2. **临时禁用 hook**：

   ```bash
   # 重命名 hook
   mv .husky/pre-push .husky/pre-push.disabled

   # 推送
   git push origin master

   # 恢复 hook
   mv .husky/pre-push.disabled .husky/pre-push
   ```

3. **通过 Pull Request**（推荐）：
   - 推送到功能分支
   - 创建 PR 合并到 master

### Q2: Hook 没有生效怎么办？

**A:** 检查以下几点：

1. **确认 husky 已安装**：

   ```bash
   npm install
   ```

2. **检查文件权限**：

   ```bash
   chmod +x .husky/pre-push
   chmod +x scripts/branch-protection.js
   ```

3. **检查 Git hooks 路径**：

   ```bash
   git config core.hooksPath
   # 应该输出：.husky
   ```

4. **手动设置 hooks 路径**：
   ```bash
   git config core.hooksPath .husky
   ```

### Q3: 如何在 CI/CD 中使用？

**A:** CI/CD 环境通常不需要这个保护（因为有其他权限控制），可以：

1. **在 CI 中跳过 hooks**：

   ```bash
   git push --no-verify
   ```

2. **检测 CI 环境**：
   在 `branch-protection.js` 开头添加：
   ```javascript
   // 在 CI 环境中跳过检查
   if (process.env.CI) {
     console.log('CI 环境，跳过分支保护检查');
     process.exit(0);
   }
   ```

### Q4: 如何保护更多分支？

**A:** 修改 `scripts/branch-protection.js` 中的配置：

```javascript
const PROTECTED_BRANCHES = {
  exact: ['master', 'main', 'production', 'staging'],
  prefixes: ['release/', 'release-', 'hotfix/', 'prod-'],
};
```

### Q5: 能否只在特定情况下保护分支？

**A:** 可以添加更复杂的逻辑，例如：

```javascript
// 只在工作日保护
const now = new Date();
const isWeekend = now.getDay() === 0 || now.getDay() === 6;
if (isWeekend) {
  console.log('周末，跳过分支保护');
  process.exit(0);
}

// 只对特定用户保护
const currentUser = require('os').userInfo().username;
const exemptUsers = ['admin', 'ci-bot'];
if (exemptUsers.includes(currentUser)) {
  console.log('豁免用户，跳过分支保护');
  process.exit(0);
}
```

## 技术细节

### 依赖项

- **Node.js**: 运行脚本
- **Git**: 提供 hooks 机制
- **Husky**: 管理 Git hooks
- **readline**: Node.js 内置模块，读取标准输入

### 文件结构

```
my-react-valtio-app/
├── .husky/
│   ├── pre-push                    # Git pre-push hook
│   └── pre-commit                  # 已有的 pre-commit hook
├── scripts/
│   ├── branch-protection.js        # 分支保护核心逻辑
│   └── test-branch-protection.js   # 测试脚本
├── package.json                    # 包含 husky 配置
└── BRANCH_PROTECTION.md            # 本文档
```

### 性能考虑

- **执行时间**: < 100ms（通常在 10-50ms）
- **内存占用**: < 10MB
- **对推送的影响**: 几乎无感知

### 安全性

- ✅ 本地保护，防止误操作
- ✅ 可以被绕过（`--no-verify`），但需要明确意图
- ⚠️ 不能替代服务器端的分支保护规则
- ⚠️ 建议在 GitHub/GitLab 上也配置分支保护

## 最佳实践

1. **结合服务器端保护**：
   - 在 GitHub/GitLab 上设置分支保护规则
   - 本地 hook 作为第一道防线
   - 服务器端规则作为最终保障

2. **团队规范**：
   - 在团队文档中说明分支保护策略
   - 培训新成员正确的工作流程
   - 定期审查和更新保护规则

3. **监控和审计**：
   - 记录绕过保护的操作
   - 定期检查直接推送到主分支的情况
   - 使用 Git 日志分析工具

4. **持续改进**：
   - 收集团队反馈
   - 根据实际情况调整保护规则
   - 保持工具的更新和维护

## 相关资源

- [Git Hooks 官方文档](https://git-scm.com/docs/githooks)
- [Husky 官方文档](https://typicode.github.io/husky/)
- [GitHub 分支保护规则](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/about-protected-branches)

## 许可证

MIT License

---

**维护者**: 项目团队
**最后更新**: 2024
